import { Link, useLocation } from 'react-router-dom';

const links = [
  { path: '/disease' },
  { path: '/quality'  },
  { path: '/export' },
  { path: '/environment' },
];

const TempAINav = () => {
  const location = useLocation();

  return (
   null
  );
};

export default TempAINav;